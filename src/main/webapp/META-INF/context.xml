<?xml version="1.0" encoding="UTF-8"?>
<Context>
    <!-- PostgreSQL example, remember to include the PostgreSQL jar in the servlet container lib folder
         This example uses the Tomcat JDBC Connection pool, change factory parameter for other implementations -->
    <!--
    <Resource name="jdbc/db"
              auth="Container"
              type="javax.sql.DataSource"
              factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
              defaultAutoCommit="true"
              driverClassName="org.postgresql.Driver"
              url="jdbc:postgresql://host/dbname?user=dbuser&amp;password=dbpassword"/>
    -->

    <!-- MySQL example, remember to include the MySQL jar in the servlet container lib folder.
         This example uses the Tomcat JDBC Connection pool, change factory parameter for other implementations -->
    <!--
    <Resource name="jdbc/db"
              auth="Container"
              type="javax.sql.DataSource"
              factory="org.apache.tomcat.jdbc.pool.DataSourceFactory"
              defaultAutoCommit="true"
              driverClassName="com.mysql.jdbc.Driver"
              url="jdbc:mysql://host/dbname?user=dbuser&amp;password=dbpassword"/>
    -->
</Context>